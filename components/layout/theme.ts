import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff9e9e',
    },
    secondary: {
      main: '#b1e0da',
    },
  },
})

export default theme
