import React from 'react'
import Container from '../common/Container'
import { ILayout } from '../layout/layout'

const Platform = ({ children }: ILayout) => <Container>{children}</Container>

export default Platform
